<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpotifyUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/backgroundColorDark</item>
        <item name="android:fontFamily">@font/mulish_extrabold</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorPrimary">@color/backgroundColorDark</item>
    </style>

    <style name="Theme.SpotifyUI" parent="Base.Theme.SpotifyUI" />
</resources>